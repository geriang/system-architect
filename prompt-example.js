let prompt = require('prompt-sync')();

// let firstName = prompt("Please enter your first name: ");
// console.log("Hi", firstName);

// let length = prompt("Please enter the length of the area in meters:");
// let width = prompt("Please enter the width of the area in meters:");
// area_of_square = length * width;
// perimeter = 2*length + 2*width;
// console.log("The area is", area_of_square)
// console.log("The perimeter is", perimeter)

// let x = 3
// let y = "4"
// let z = 5

// ans = z+parseInt(y+x)
// console.log(ans) 

// let x=-5;
// while (x<=5) {
//     console.log(x)
//     x=x+1
// }

// Lesson 1 on Javascript (19 Oct 2022)

// How to represent data
// basic data types: strings, numbers (integers, floats), boolean

// examples of strings:
// "Paul"
// 'Chor'

// Operators
// 1. Operators - symbols that represent instructions : + - * /  
// 2. Functions - MDN Web Docs

// Fuctions
// function consists of a name and contextual data and usually gives a result 

// calling a fuction => executing the parameters (also argument) in the bracket 
// return value is the result of a function
// the parameters of a function must all be simple values before they can be executed.


// Variables
// variables are storage for data. Data that are not stored in a variable is not remembered by Javascript

// 1. naming a variable 
// a) cannot begin with a number, b) cannot use a reserved word, e.g. if, else, c) case sensitive d) all other symbols are not allowed, including spaces e) _ and $ are ok symbols


// Imperative
// Imperative vs declarative (CSS) 
// computer/cpu executes the code line by line and after that it is forgotten


// Unusual values
// NaN = Not a Number ; undefined + anything = NaN ; undefined + 3 = NaN
// null = "nothing" - sometimes null has to be delibrately assigned (sometimes the variable needs to be defined later, hence null first)




